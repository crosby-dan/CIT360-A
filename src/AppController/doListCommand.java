package AppController;

/*@author Dan Crosby
* CIT360
* An interface that defines the handoff process for the application controller.
*/
public interface doListCommand {
    void runCommand(String sCommand);
}
